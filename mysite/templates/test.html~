<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>test</title>
</head>
<body>
    <h2>test</h2>
    {% for author in authors %}
    <p>{{ author.authorID }}</p>
    <p>{{ author.name }}</p>
    <p>{{ author.age }}</p>
    <p>{{ author.country }}</p>
    {% endfor %}
</body>
</html>
